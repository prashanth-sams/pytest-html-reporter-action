## What this changes

<!-- and why -->

## Checklist

- [ ] `python -m pytest` passes
- [ ] A new or renamed input has its README row (`test_action_yml.py` checks this)
- [ ] A change to `action.yml` has been through `self-test.yml` on a push
- [ ] `CHANGELOG.md` has an entry, if this is user-visible
